# 待办问题清单

全部完成（2026-09-20 复核）：以下 5 项均已落地，记录保留。

## 1. 墨迹未干提示位置 ✅

热重载提示已重做为「印章」式：悬浮于窗口中下方（水平居中、距底约 22%），不随文档滚动，所有窗口尺寸可见。侧边栏打开时随文章区域中心右移（≥720px 断点）。动画为放大旋转→按压回弹→停留→淡出，正文保留由虚而实的新墨过渡。

## 2. 性能优化 ✅

- **字体策略收紧**：`waitForFonts()` 增加 `document.fonts.check()` 快路径、改用 `Promise.allSettled`（单个失败不影响整体）、兜底超时 3s→1s；保留 `font-display: block` 防 FOUT。
- **合并 Store**：新建 `src/lib/settings.ts` 共享 settings.json Store 单例，`lastOpened` 与 `outlineOpen` 复用，消除重复 IPC。
- **代码分割**：`MarkdownDocument` 改为 `React.lazy` 懒加载，react-markdown + rehype/remark + 语法高亮拆为独立 chunk（主包从约 600kB 降至 320kB）。
- **启动跳过中间 loading 态**：首次加载直接 empty → ready，少一次无意义渲染。
- 启动闪现旧界面此前已解决，本次未涉及。

## 3. 大纲面板未跟随滚动 ✅

`OutlinePanel` 在活跃标题变化时把高亮项平滑滚进视野（`block: 'nearest'` 语义）。原生 `scrollIntoView` 在高频变化时会被连续打断（近距离慢挪、远距离直跳），故改为手动计算目标位置 + 自定义缓动从当前位置接续动画。

## 4. 全屏 + 侧边栏文字宽度异常 ✅

移除了侧边栏打开时正文加宽到 960px 的规则（及 `--content-max-width-wide` 变量）。无论侧边栏开关，正文最大宽度恒为 800px。（2026-09-20 起 800px 是**可调阅读设置的默认值**——用户可在顶栏齿轮里选 720 / 800 / 960；侧栏开关依旧不影响栏宽。）

## 5. 默认侧边栏应关闭 ✅

`useOutlineOpen` 启动时恒为关闭，不再恢复上次的开关状态；用户交互后的状态仍会持久化（仅启动时不读取）。

## 阅读器完善计划（2026-09-20）✅

来源：2026-09-20 代码评审建议，用户逐条确认。计划全文与逐任务报告在 `docs/superpowers/plans/2026-09-20-reader-polish.md` 与 `.superpowers/sdd/2026-09-20-reader-polish/`（12 个任务全部完成，终验 fix wave 与文档收口见 `CHANGELOG.md` 的 1.9.0 条目）。

- **阅读设置面板**：顶栏齿轮调字号 / 栏宽 / 行高，落盘并覆写根 CSS 变量（初版是顶栏弹层，2026-09-20 第二批改为替换正文区的设置页，见下）。
- **最近打开 + 拖放打开 + 空状态重做**：启动回到上一篇，空态/错误页共用最近 8 篇列表，`.md` 可直接拖进窗口。
- **窗口标题随文档**：OS 级 `setTitle`，顶栏仍不显示文件名。
- **wikilink 前进/后退**：`Alt+←` / `Alt+→` 与顶栏 ‹ ›，回到上一篇的原位。
- **大纲收录 h4–h6**：深层级降档显示，编号仍只给 h1。
- **任务列表勾选写回**：阅读视图点复选框直接改源码（乐观更新 + 失败回滚 + mdlog 门禁）。
- **打印样式**：两段 `@media print`（隐藏界面件 / 放开版心 / 分页保护）。`Ctrl+P` 已绑定，**真机未验证**（README 暂不写这条快捷键）。
- **自动更新**：`tauri-plugin-updater` 接入，生产构建启动时静默检查；发布前须生成签名密钥并填 `pubkey`（当前为占位）。
- **UI 修复批量**：`Ctrl+B` 切侧栏、错误页「重新打开」、搜索清除按钮与「无匹配」、拖宽手柄窄屏守卫、纱罩 z 序、聚焦描边、顶栏「记录中」小章。

## 设置页 / 顶栏 B / 勾选定稿（2026-09-20 第二批）✅

来源：界面提案预览 `docs/preview/topbar-settings-proposal.html`（定稿版，用户逐节拍板）。计划全文与逐任务报告在 `docs/superpowers/plans/2026-09-20-settings-page-topbar.md` 与 `.superpowers/sdd/2026-09-20-settings-page-topbar/`（4 个任务全部完成；审阅修复与文档收口见 `CHANGELOG.md` 的 1.9.0 条目）。

- **顶栏 B（纯工具栏）**：路径退出顶栏（`.top-bar__path` / `.top-bar__meta` 删除，中列只剩拖动热区），左簇重排为 大纲 → ‹ → › → 编辑 → 打开文件 → 分隔线 → 齿轮 →「记录中」小章；完整路径改挂正文 `h1.document-title` 的 `title` tooltip。
- **设置页（替换正文区，弹层退役）**：顶栏齿轮进入设置视图，正文区整块换成四节内容栏（阅读 / 界面 / 更新 / 关于与数据），侧栏换成「設定」分节导航（复用同一枚 `.outline-sidebar`，开合与拖宽入口不变）；`Esc` 或「‹ 返回阅读」退出，退出后回到进入前的阅读位置。`SettingsPopover` 弹层删除。
- **勾选定稿（钤印 + 划线）**：自绘 15px 方框、已勾填 `--tag-bg` + 靛青对勾、同项文字灰化 + 1px 删除线（打印下保持可读）；只作用阅读视图。
